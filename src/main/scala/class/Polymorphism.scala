package `class`

class Polymorphism {
  /**
    * Polymorphism means that a function type comes "in may forms". In programming it means that:
    *   1: the function can be applied to arguments of many types, or
    *   2: The type can have instances of many types
    *
    *   two principal forms of polymorphism
    *   subtyping: instance of a subclss can be passed to a base class
    *   generics: instances of a function or class are created by type parameterization
    *
    */
}
